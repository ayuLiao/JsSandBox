navigator = box.proxy(navigator, "navigator")
document = box.proxy(document, "document")
localStorage = box.proxy(localStorage, "localStorage")

location = box.proxy(location, "location")
history = box.proxy(history, "history")
window = box.proxy(window, "window")
screen = box.proxy(screen, 'screen')